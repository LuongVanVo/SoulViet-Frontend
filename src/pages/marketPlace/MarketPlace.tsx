import { MarketplaceFeature } from '@/features/marketPlace'

export const MarketPlace = () => {
  return (
    <div className="animate-in fade-in duration-500">
      <MarketplaceFeature />
    </div>
  )
}